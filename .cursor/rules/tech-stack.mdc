---
description: 技术栈规范，定义项目使用的技术栈、工具链和最佳实践
globs:
alwaysApply: true
---
# 技术栈规范

## 技术栈概览

本项目采用现代化的后端 API 技术栈，基于 **Node.js** 运行时构建，具有类型安全、性能优异和易维护的特点。

### 架构特点
- **类型安全**：全栈 TypeScript 开发，使用 Zod 进行运行时验证
- **API 文档**：自动生成 OpenAPI 规范文档
- **结构化日志**：使用 Pino 进行结构化日志记录
- **类型化数据库**：使用 Drizzle ORM 提供类型安全的数据库访问
- **边缘数据库**：使用 Turso（基于 SQLite）作为数据库

## 后端技术栈 (API)

### 核心框架
- **Hono** `^4.8.12` - 轻量级、高性能的 Web 框架
- **@hono/zod-openapi** `^1.0.2` - 类型安全的 OpenAPI 集成
- **@hono/node-server** `^1.18.1` - Node.js 运行时支持

### 数据库与 ORM
- **Drizzle ORM** `^0.44.4` - 类型安全的 TypeScript ORM
- **drizzle-zod** `^0.8.2` - Zod 验证器集成，自动生成 Zod 模式
- **@libsql/client** `^0.15.10` - Turso 数据库客户端（基于 SQLite）
- **drizzle-kit** `^0.31.4` - 数据库迁移和模式管理工具

### 数据验证
- **Zod** `^4.0.14` - TypeScript 优先的运行时类型验证库
  - 用于请求/响应验证
  - 环境变量验证
  - 数据库模式验证

### 日志与监控
- **Pino** `^9.7.0` - 高性能结构化日志库
- **hono-pino** `^0.10.1` - Pino 与 Hono 的集成中间件
- **pino-pretty** `^13.1.1` - 开发环境下的日志美化工具

### 开发工具
- **Stoker** `2.0.1` - 减少样板代码的工具库
  - 提供常用的中间件（错误处理、404 处理等）
  - 提供 OpenAPI 钩子函数
  - 减少重复代码

### API 文档
- **@scalar/hono-api-reference** `^0.9.13` - 交互式 API 文档生成
  - 基于 OpenAPI 规范自动生成文档
  - 支持交互式 API 测试
  - 美观的文档界面

### 环境变量管理
- **dotenv** `^17.2.1` - 环境变量加载
- **dotenv-expand** `^12.0.2` - 支持变量扩展的 dotenv

## 开发工具与配置

### 包管理
- **pnpm** - 快速、节省磁盘空间的包管理器
  - 使用 workspace 功能管理内部依赖
  - 高效的依赖解析和安装

### 代码质量
- **ESLint** `^9.32.0` - 代码检查工具
- **@antfu/eslint-config** `^5.1.0` - 现代化 ESLint 配置
  - 包含 TypeScript、格式化、风格检查等规则
  - 自动修复常见问题
- **eslint-plugin-format** `^1.0.1` - 代码格式化插件

### TypeScript 配置
- **TypeScript** `^5.8.3` - 类型系统
- **@types/node** `^24.2.0` - Node.js 类型定义

### 开发运行时
- **tsx** `^4.19.4` - TypeScript 执行工具
  - 支持直接运行 TypeScript 文件
  - 支持 watch 模式进行热重载
  - 无需编译即可运行代码

### 构建工具
- **tsc-alias** `^1.8.16` - TypeScript 路径别名解析
  - 编译后替换路径别名
  - 支持 `@/*` 等路径别名

### 测试框架
- **Vitest** `^3.2.1` - 快速单元测试框架
  - 基于 Vite 构建
  - 支持 ESM 和 TypeScript
  - 快速执行和热更新
- **cross-env** `^10.0.0` - 跨平台环境变量设置

## 部署与基础设施

### 运行时
- **Node.js** - JavaScript 运行时
  - 支持 ES Modules
  - 使用 `@hono/node-server` 作为 HTTP 服务器

### 数据库
- **Turso** - 基于 SQLite 的边缘数据库
  - 支持边缘计算
  - 低延迟全球分布
  - SQLite 兼容
  - 支持本地开发（SQLite）和生产环境（Turso）

### 数据库迁移
- 使用 Drizzle Kit 管理数据库迁移
- 支持生成迁移文件和应用迁移
- 支持数据库模式推送（push）和拉取（pull）

## 技术栈最佳实践

### 1. 类型安全
- **编译时类型检查**：使用 TypeScript 严格模式
- **运行时验证**：使用 Zod 验证所有外部输入（请求参数、环境变量等）
- **数据库类型推断**：利用 Drizzle ORM 的类型推断功能
- **路由类型安全**：使用 `@hono/zod-openapi` 确保路由类型安全

### 2. 代码组织
- **单一职责原则**：每个文件只负责一个功能
- **模块化设计**：按功能组织代码结构
- **路径别名**：使用 `@/*` 路径别名简化导入
- **DRY 原则**：使用 Stoker 等工具减少样板代码

### 3. API 设计
- **OpenAPI 优先**：所有路由使用 OpenAPI 规范定义
- **自动文档生成**：路由定义自动生成 API 文档
- **类型安全的路由**：使用 Zod 模式定义请求和响应
- **统一的错误处理**：使用 Stoker 的中间件处理错误

### 4. 日志记录
- **结构化日志**：使用 Pino 进行结构化日志记录
- **日志级别**：合理使用不同的日志级别（debug、info、warn、error）
- **请求 ID**：使用 `request-id` 中间件追踪请求
- **性能考虑**：Pino 高性能，适合生产环境

### 5. 数据库操作
- **模式定义优先**：使用 Drizzle 定义数据库模式
- **迁移管理**：使用 Drizzle Kit 管理数据库迁移
- **类型安全查询**：利用 Drizzle 的类型推断
- **Zod 集成**：使用 drizzle-zod 自动生成 Zod 模式

### 6. 环境变量管理
- **类型安全**：使用 Zod 定义环境变量模式
- **验证必需变量**：应用启动时验证所有必需的环境变量
- **环境区分**：支持不同环境使用不同的配置文件
- **安全实践**：不在日志中记录敏感信息

### 7. 测试编写
- **单元测试**：为每个路由模块编写单元测试
- **测试应用**：使用 `createTestApp()` 创建测试应用
- **测试隔离**：每个测试应该独立运行
- **测试数据**：使用测试专用的数据库配置

### 8. 开发体验
- **热重载**：使用 `tsx watch` 进行开发热重载
- **类型检查**：使用 `pnpm typecheck` 进行类型检查
- **代码格式化**：使用 ESLint 自动格式化代码
- **路径别名**：使用 `@/*` 简化导入路径

### 9. 错误处理
- **统一错误格式**：使用 Stoker 的 `onError` 中间件
- **404 处理**：使用 Stoker 的 `notFound` 中间件
- **错误日志**：记录详细的错误信息
- **用户友好**：返回用户友好的错误消息

### 10. 性能优化
- **轻量级框架**：Hono 框架性能优异
- **结构化日志**：Pino 高性能日志库
- **数据库优化**：使用 Turso 边缘数据库减少延迟
- **查询优化**：避免 N+1 查询问题

## 版本管理策略

### 依赖更新原则
- **定期更新**：定期检查依赖更新
- **稳定版本**：优先使用稳定版本
- **测试兼容性**：更新后测试兼容性
- **版本锁定**：使用 `^` 允许补丁和次版本更新

### 技术栈演进
- **关注新技术**：关注相关技术的更新和新特性
- **评估成本**：评估升级的成本和收益
- **保持现代性**：保持技术栈的现代化
- **文档化决策**：记录重要的技术决策

## 开发环境要求

### 必需工具
- **Node.js** >= 18.0.0（推荐 >= 20.0.0）
- **pnpm** >= 8.0.0
- **Git** >= 2.30.0

### 推荐工具
- **VS Code** - 代码编辑器
  - 推荐插件：ESLint、TypeScript、Prettier
- **Drizzle Studio** - 数据库管理工具
  - 可视化数据库管理
  - 支持数据浏览和编辑

### 环境配置
- **环境变量**：配置 `.env` 文件
  - `DATABASE_URL`：数据库连接 URL
  - `DATABASE_AUTH_TOKEN`：数据库认证令牌（生产环境）
  - `LOG_LEVEL`：日志级别
  - `PORT`：服务器端口（默认：9999）
  - `NODE_ENV`：运行环境（development、test、production）

## 脚本说明

### 开发脚本
- `pnpm dev` - 启动开发服务器（使用 tsx watch 热重载）
- `pnpm start` - 启动生产服务器（运行编译后的代码）

### 构建脚本
- `pnpm build` - 构建项目（编译 TypeScript 并解析路径别名）
- `pnpm typecheck` - 类型检查（不生成文件）

### 测试脚本
- `pnpm test` - 运行测试（使用 Vitest）

### 代码质量脚本
- `pnpm lint` - 检查代码质量（使用 ESLint）
- `pnpm lint:fix` - 自动修复代码问题

### 数据库脚本
- `pnpm drizzle-kit push` - 推送数据库模式（开发环境）
- `pnpm drizzle-kit generate` - 生成数据库迁移文件
- `pnpm drizzle-kit migrate` - 应用数据库迁移

## API 文档访问

### 文档端点
- `GET /doc` - OpenAPI 规范文档（JSON 格式）
- `GET /reference` - Scalar API 文档界面（交互式文档）

### 文档特性
- 自动生成：基于路由定义自动生成
- 类型安全：与 TypeScript 类型系统集成
- 交互式测试：支持直接在文档中测试 API
- 美观界面：使用 Scalar 提供的现代化界面

## 技术栈选择理由

### Hono
- 轻量级、高性能
- 支持多种运行时（Node.js、Cloudflare Workers、Bun 等）
- 类型安全的路由系统
- 活跃的社区和生态

### Drizzle ORM
- 类型安全的 ORM
- 与 TypeScript 深度集成
- 支持多种数据库
- 优秀的性能

### Zod
- TypeScript 优先的验证库
- 运行时类型验证
- 与 TypeScript 类型系统无缝集成
- 强大的错误信息

### Pino
- 高性能结构化日志
- 生产环境友好
- 丰富的生态系统
- 易于集成

### Vitest
- 快速执行
- 基于 Vite 构建
- 原生支持 TypeScript 和 ESM
- 优秀的开发体验
